name: Repo Policy

on:
  push:
  pull_request:

jobs:
  verify-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify repository policy
        run: bash scripts/verify-policy.sh
